{
  "name": "logonsystem",
  "version": "1.1.0",
  "private": true,
  "description": "Authentication-focused application for studying login flows, validation, sessions and secure application structure.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/security.js && node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0"
  },
  "keywords": [
    "authentication",
    "security",
    "nodejs",
    "sessions",
    "validation"
  ],
  "author": "Gabriel de Lima Cardoso"
}
